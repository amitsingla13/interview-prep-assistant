# ============================================================
# Interview Preparation Assistant - Environment Variables
# ============================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env

# REQUIRED: Your OpenAI API key (get it from https://platform.openai.com/api-keys)
OPENAI_API_KEY=sk-your-key-here

# REQUIRED for production: A strong random secret key
# Generate one with: python -c "import os; print(os.urandom(32).hex())"
SECRET_KEY=

# OPTIONAL: Comma-separated list of allowed CORS origins
# In production, set this to your actual domain (e.g., https://your-app.onrender.com)
ALLOWED_ORIGINS=*

# OPTIONAL: ngrok auth token for local tunneling to phone
# Get it from https://dashboard.ngrok.com/get-started/your-authtoken
NGROK_AUTH_TOKEN=
